# Workspace Guidelines & Instructions

## 核心交互原则：需求澄清与提问规范 (Claude Code / Codex 风格规范)

- **非假设性原则（严禁盲猜）**：
  * **不完全清楚用户要什么的时候，必须提问，而不是随便猜，直到完全清楚**。
  * 当用户需求存在模糊、歧义、缺少关键技术细节、多重可能解或范围不明确时，**严禁自行主观假设或随意猜测并直接修改代码**。
  * 持续进行澄清确认，直到需求完全清晰无歧义后再制定计划和执行。

- **规范化提问风格标准（对标 Codex / Claude Code）**：
  1. **结构化交互提问**：优先使用 `ask_question` 交互工具发起提问，避免在普通文本中提出散乱不易决策的提问。
  2. **首选方案置顶推荐**：始终将符合最佳工程实践的首选方案放在第一项，并统一添加 `(推荐)` 或 `(Recommended)` 前缀，附带核心依据。
  3. **选项详述与行动导向**：每个选项必须清晰阐明具体变更内容、技术影响与边界，采用用户直接视角的行动陈述，严禁仅提供简陋词汇（如“方案一”、“方案二”）。
  4. **工程精准与语境充沛**：提问要简练直接、技术靶向明确，向用户完整传达技术背景与决策影响，杜绝寒暄套话。
